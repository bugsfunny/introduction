import { NoOpenDirective } from './no-open.directive';

describe('NoOpenDirective', () => {
  it('should create an instance', () => {
    const directive = new NoOpenDirective();
    expect(directive).toBeTruthy();
  });
});
